---
title: "FinalProject"
author: "Ben Lehmann"
date: "2024-12-06"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:


```{r}
library(dplyr)
library(tidyr)
library(tidyverse)
life  <- read.csv('Life Expectancy Data.csv')
data_2015 <- filter(life,Year == "2015")
data_2015 <- data_2015 %>% select(-Total.expenditure)
data_2015 <- data_2015 %>% select(-Alcohol)
data_2015 <- data_2015 %>% drop_na()
data_2015 <- data_2015 %>% mutate_at(vars(Country, Year, Status), as.factor)
```



f).




g).

```{r}
library(factoextra)
library(cluster)
```

```{r}
new_data <- subset(data_2015, select=-c(Country, Status,Year))

df_scaled<- scale(new_data)
df_scaled=as.data.frame(df_scaled)
```

```{r}
distance_matrix <- dist(df_scaled, method = "euclidean")
```


```{r}
fviz_nbclust(df_scaled, kmeans, method = "wss")
```

```{r}
set.seed(1)

#perform k-means clustering with k = 4 clusters
km <- kmeans(df_scaled, centers = 4, nstart = 25)
```


```{r}
fviz_cluster(km, data = df_scaled)
```

